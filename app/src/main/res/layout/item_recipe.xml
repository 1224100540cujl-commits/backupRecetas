<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Imagen de fondo con gradiente -->
        <View
            android:id="@+id/recipeImageBackground"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:background="@drawable/bg_recipe_image"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Emoji de la receta -->
        <TextView
            android:id="@+id/recipeEmoji"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🌮"
            android:textSize="48sp"
            app:layout_constraintBottom_toBottomOf="@id/recipeImageBackground"
            app:layout_constraintEnd_toEndOf="@id/recipeImageBackground"
            app:layout_constraintStart_toStartOf="@id/recipeImageBackground"
            app:layout_constraintTop_toTopOf="@id/recipeImageBackground" />

        <!-- Contenedor de información -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp"
            app:layout_constraintTop_toBottomOf="@id/recipeImageBackground">

            <!-- Nombre de la receta -->
            <TextView
                android:id="@+id/recipeNameTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Tacos al Pastor"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/black"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginBottom="8dp" />

            <!-- Información de tiempo y porciones -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:src="@drawable/ic_time"
                    app:tint="@color/gray_medium" />

                <TextView
                    android:id="@+id/recipeTimeTextView"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:text="30 min"
                    android:textSize="12sp"
                    android:textColor="@color/gray_medium" />

                <ImageView
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_marginStart="12dp"
                    android:src="@drawable/ic_people"
                    app:tint="@color/gray_medium" />

                <TextView
                    android:id="@+id/recipeServingsTextView"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:text="4"
                    android:textSize="12sp"
                    android:textColor="@color/gray_medium" />

            </LinearLayout>

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>