<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp">

        <!-- Emoji -->
        <TextView
            android:id="@+id/favoriteRecipeEmoji"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:background="@drawable/bg_recipe_image"
            android:gravity="center"
            android:text="🌮"
            android:textSize="40sp" />

        <!-- Información -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/favoriteRecipeName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Tacos al Pastor"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/black"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginBottom="4dp" />

            <TextView
                android:id="@+id/favoriteRecipeCategory"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Mexicana"
                android:textSize="14sp"
                android:textColor="@color/gray_medium"
                android:layout_marginBottom="8dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_time"
                    app:tint="@color/gray_medium" />

                <TextView
                    android:id="@+id/favoriteRecipeTime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:text="30 min"
                    android:textSize="12sp"
                    android:textColor="@color/gray_medium" />

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_marginStart="16dp"
                    android:src="@drawable/ic_people"
                    app:tint="@color/gray_medium" />

                <TextView
                    android:id="@+id/favoriteRecipeServings"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:text="4"
                    android:textSize="12sp"
                    android:textColor="@color/gray_medium" />

            </LinearLayout>

        </LinearLayout>

        <!-- Botón de favorito -->
        <ImageButton
            android:id="@+id/removeFavoriteButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_favorite"
            android:contentDescription="Eliminar de favoritos"
            app:tint="@color/red_favorite" />

    </LinearLayout>

</androidx.cardview.widget.CardView>